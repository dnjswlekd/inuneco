const swiper = new Swiper('.swiper-container', {
    // Optional parameters
    speed: 500,
    loop: true,
  
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },
  
    // Navigation arrows
    navigation: {
      nextEl: '.Q1-btn',
    }
  });